--- /mnt/data/l26_alarmas_build/l26_filter_core.before_alarm.js	2026-09-02 07:15:49.232865716 +0000
+++ app/assets/l26_filter_core.js	2026-09-02 07:15:49.701015970 +0000
@@ -21,5 +21,9 @@
     const year=String(selectedYear||'').trim();
     return year?list.filter(item=>caseTramiteYear(item)===year):list.slice();
   }
-  return{caseTramiteYear,yearEntries,filterByYear};
+  function filterByAlarm(cases,alarmOnly=false,needsAlarm=()=>false){
+    const list=Array.isArray(cases)?cases:[];
+    return alarmOnly?list.filter(item=>Boolean(needsAlarm(item))):list.slice();
+  }
+  return{caseTramiteYear,yearEntries,filterByYear,filterByAlarm};
 });
--- /mnt/data/l26_alarmas_build/index.before_alarm_filter.html	2026-09-02 07:15:49.230815414 +0000
+++ index.html	2026-09-02 07:15:49.774469854 +0000
@@ -434,8 +434,8 @@
 /* V22.05 PRUEBA R2 — corrección visible de selecciones, filtro agro y consecutivo automático de inspección de campo; base V22.0.4 */
 .base-local-filter-panel{display:grid;grid-template-columns:minmax(240px,1.8fr) minmax(125px,.7fr) minmax(170px,1fr) minmax(190px,1fr);gap:10px;align-items:end;margin:12px 0;padding:12px;border:1px solid #b9cec3;border-radius:14px;background:#f7fbf9}
 .base-local-filter-panel h3{grid-column:1/-1;margin:0;color:#174f37;font-size:.95rem}.base-local-filter-panel .field{margin:0}.base-local-filter-panel input,.base-local-filter-panel select{min-height:38px}.base-local-filter-status{grid-column:1/-1;margin:0;color:#52645b;font-size:.75rem;font-weight:700}
-.base-local-agro-filter{grid-column:1/-1;display:grid;grid-template-columns:auto minmax(220px,max-content) 1fr;align-items:center;gap:8px 10px;padding:9px 10px;border:1px solid #a9c78a;border-radius:10px;background:#f3f9ea}.base-local-agro-filter>span{font-size:.72rem;font-weight:900;color:#3e5e2d;text-transform:uppercase;letter-spacing:.04em}.base-local-agro-filter>small{font-size:.68rem;color:#5c6d54}.agro-filter-btn{display:inline-flex;align-items:center;justify-content:center;gap:7px;min-height:34px!important;padding:6px 11px!important;border:1px solid #6f9e4e!important;background:#e8f5d9!important;color:#315b18!important;box-shadow:none!important;font-weight:900!important}.agro-filter-btn:before{content:"🌱"}.agro-filter-btn[aria-pressed="true"]{background:#47772a!important;color:#fff!important;border-color:#47772a!important;box-shadow:0 0 0 2px rgba(71,119,42,.13)!important}.agro-filter-count{display:inline-grid;place-items:center;min-width:22px;height:20px;padding:0 5px;border-radius:999px;background:#fff;color:#375b1d;font-size:.65rem;font-weight:900}.agro-filter-btn[aria-pressed="true"] .agro-filter-count{background:#fff;color:#375b1d}.agro-badge{display:inline-flex;align-items:center;padding:3px 7px;border-radius:999px;background:#edf7dc;color:#3b641f;border:1px solid #aacb83;font-size:.62rem;font-weight:900;white-space:nowrap}.report-id-card{border-left:4px solid var(--green)}.report-id-card .field input{font-weight:850;color:#174f37;background:#f4f8f6}.report-id-help{grid-column:1/-1;font-size:11px;color:#65746c;margin:0}
-#view-management .base-local-filter-panel{grid-template-columns:minmax(190px,1.5fr) repeat(5,minmax(115px,.8fr))}#view-management .base-local-agro-filter{grid-column:1/-1}@media(max-width:1050px){#view-management .base-local-filter-panel{grid-template-columns:repeat(3,minmax(0,1fr))}}@media(max-width:760px){.base-local-filter-panel{grid-template-columns:1fr}.base-local-filter-panel h3,.base-local-filter-status,.base-local-agro-filter{grid-column:1}.base-local-agro-filter{grid-template-columns:1fr}.base-local-agro-filter .agro-filter-btn{width:100%}}
+.base-local-agro-filter{grid-column:1/-1;display:grid;grid-template-columns:auto minmax(220px,max-content) 1fr;align-items:center;gap:8px 10px;padding:9px 10px;border:1px solid #a9c78a;border-radius:10px;background:#f3f9ea}.base-local-agro-filter>span{font-size:.72rem;font-weight:900;color:#3e5e2d;text-transform:uppercase;letter-spacing:.04em}.base-local-agro-filter>small{font-size:.68rem;color:#5c6d54}.agro-filter-btn{display:inline-flex;align-items:center;justify-content:center;gap:7px;min-height:34px!important;padding:6px 11px!important;border:1px solid #6f9e4e!important;background:#e8f5d9!important;color:#315b18!important;box-shadow:none!important;font-weight:900!important}.agro-filter-btn:before{content:"🌱"}.agro-filter-btn[aria-pressed="true"]{background:#47772a!important;color:#fff!important;border-color:#47772a!important;box-shadow:0 0 0 2px rgba(71,119,42,.13)!important}.agro-filter-count{display:inline-grid;place-items:center;min-width:22px;height:20px;padding:0 5px;border-radius:999px;background:#fff;color:#375b1d;font-size:.65rem;font-weight:900}.agro-filter-btn[aria-pressed="true"] .agro-filter-count{background:#fff;color:#375b1d}.quick-filter-buttons{display:flex;flex-wrap:wrap;gap:8px}.alarm-filter-btn{display:inline-flex;align-items:center;justify-content:center;gap:7px;min-height:34px!important;padding:6px 11px!important;border:1px solid #c38a22!important;background:#fff5dd!important;color:#79500a!important;box-shadow:none!important;font-weight:900!important}.alarm-filter-btn:before{content:"⚠"}.alarm-filter-btn[aria-pressed="true"]{background:#a66b00!important;color:#fff!important;border-color:#a66b00!important;box-shadow:0 0 0 2px rgba(166,107,0,.13)!important}.alarm-filter-count{display:inline-grid;place-items:center;min-width:22px;height:20px;padding:0 5px;border-radius:999px;background:#fff;color:#79500a;font-size:.65rem;font-weight:900}.alarm-filter-btn[aria-pressed="true"] .alarm-filter-count{background:#fff;color:#79500a}.agro-badge{display:inline-flex;align-items:center;padding:3px 7px;border-radius:999px;background:#edf7dc;color:#3b641f;border:1px solid #aacb83;font-size:.62rem;font-weight:900;white-space:nowrap}.report-id-card{border-left:4px solid var(--green)}.report-id-card .field input{font-weight:850;color:#174f37;background:#f4f8f6}.report-id-help{grid-column:1/-1;font-size:11px;color:#65746c;margin:0}
+#view-management .base-local-filter-panel{grid-template-columns:minmax(190px,1.5fr) repeat(5,minmax(115px,.8fr))}#view-management .base-local-agro-filter{grid-column:1/-1}@media(max-width:1050px){#view-management .base-local-filter-panel{grid-template-columns:repeat(3,minmax(0,1fr))}}@media(max-width:760px){.base-local-filter-panel{grid-template-columns:1fr}.base-local-filter-panel h3,.base-local-filter-status,.base-local-agro-filter{grid-column:1}.base-local-agro-filter{grid-template-columns:1fr}.base-local-agro-filter .agro-filter-btn,.base-local-agro-filter .alarm-filter-btn{width:100%}}
 
 /* V22 — tarjetas compactas, PDF desde Base local y Gestión sin datos reiterativos */
 .base-local-actions{gap:6px!important}.base-local-actions .compact-control{min-height:28px!important;padding:4px 8px!important;border-radius:8px!important;font-size:.70rem!important;box-shadow:0 2px 5px rgba(0,0,0,.07)!important}
@@ -610,7 +610,7 @@
       <label class="field"><span>Año del trámite</span><select id="baseLocalYearFilter"><option value="">Todos los años</option></select></label>
       <label class="field"><span>Distrito</span><select id="baseLocalDistrictFilter"><option value="">Todos los distritos</option></select></label>
       <label class="field"><span>Lugar</span><select id="baseLocalPlaceFilter"><option value="">Todos los lugares</option></select></label>
-      <div class="base-local-agro-filter" aria-label="Filtro rápido por uso agro indicado en observaciones"><span>Filtro rápido</span><button type="button" class="btn agro-filter-btn" id="baseLocalAgroFilter" aria-pressed="false" title="Mostrar únicamente expedientes cuyas observaciones indiquen uso agro o agropecuario">USO AGRO <b class="agro-filter-count" id="baseLocalAgroCount">0</b></button><small>Se detecta automáticamente desde <b>Observaciones del expediente</b>. Pulse para mostrar solo esos casos.</small></div>
+      <div class="base-local-agro-filter" aria-label="Filtros rápidos de trámites"><span>Filtro rápido</span><div class="quick-filter-buttons"><button type="button" class="btn agro-filter-btn" id="baseLocalAgroFilter" aria-pressed="false" title="Mostrar únicamente expedientes cuyas observaciones indiquen uso agro o agropecuario">USO AGRO <b class="agro-filter-count" id="baseLocalAgroCount">0</b></button><button type="button" class="btn alarm-filter-btn" id="baseLocalAlarmFilter" aria-pressed="false" title="Mostrar únicamente trámites activos con alarma de más de un año">ALARMAS +1 AÑO <b class="alarm-filter-count" id="baseLocalAlarmCount">0</b></button></div><small>Los filtros rápidos se pueden combinar con búsqueda, año, distrito y lugar.</small></div>
       <p class="base-local-filter-status" id="baseLocalFilterStatus" aria-live="polite">Mostrando todos los expedientes activos.</p>
     </section>
     <div class="case-hierarchy-summary" id="caseHierarchySummary" aria-live="polite"></div>
@@ -1272,7 +1272,7 @@
 function isFieldElement(c){return isLinearUnit(c)||['Gavión','Losa','Obra exterior','Obra vial complementaria','Rampa','Portón','Poste'].includes(c?.category)}
 
 const state={current:null,audit:[],signature:{drawing:false}};
-const baseLocalFilterState={query:'',year:'',district:'',place:'',agroOnly:false};let baseLocalSearchTimer=null;
+const baseLocalFilterState={query:'',year:'',district:'',place:'',agroOnly:false,alarmOnly:false};let baseLocalSearchTimer=null;
 const caseChannel='BroadcastChannel'in window?new BroadcastChannel('fiscalizacion-bi-v22-cases'):null;
 const TAB_SESSION_ID=(()=>{let id=crypto.randomUUID?crypto.randomUUID():`${Date.now()}-${Math.random().toString(16).slice(2)}`;try{id=sessionStorage.getItem('fiscalizacion_tab_id')||id;sessionStorage.setItem('fiscalizacion_tab_id',id)}catch(_){ }return id})();
 function canonicalCaseKey(value){return String(value||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toUpperCase().replace(/[^A-Z0-9]/g,'')}
@@ -3236,9 +3236,9 @@
 function baseLocalDistrictEntries(cases){const groups=new Map();for(const c of cases){const label=String(c?.visit?.district||'Sin distrito').trim()||'Sin distrito',key=canonicalProcessGeoName(label);let g=groups.get(key);if(!g)g=[...groups.values()].find(x=>processGeoNamesMatch(key,x.key));if(!g){g={key,labels:new Map()};groups.set(key,g)}g.labels.set(label,(g.labels.get(label)||0)+1)}return [...groups.values()].map(g=>({key:g.key,label:L26TerritoryCore.formatDistrict(preferredProcessGeoName(g.labels,'Sin distrito'))})).sort((a,b)=>caseNaturalCollator.compare(a.label,b.label))}
 function baseLocalPlaceEntries(cases,districtKey=''){const groups=new Map();for(const c of cases){const dk=canonicalProcessGeoName(c?.visit?.district||'Sin distrito');if(districtKey&&!processGeoNamesMatch(dk,districtKey))continue;const label=String(casePlace(c)||'Sin poblado o lugar').trim()||'Sin poblado o lugar',key=canonicalProcessGeoName(label,{place:true});let g=groups.get(key);if(!g)g=[...groups.values()].find(x=>processGeoNamesMatch(key,x.key));if(!g){g={key,labels:new Map()};groups.set(key,g)}g.labels.set(label,(g.labels.get(label)||0)+1)}return [...groups.values()].map(g=>({key:g.key,label:L26TerritoryCore.formatPlace(preferredProcessGeoName(g.labels,'Sin poblado o lugar'))})).sort((a,b)=>caseNaturalCollator.compare(a.label,b.label))}
 function setBaseLocalSelectOptions(select,entries,allLabel,selected){if(!select)return'';select.innerHTML=`<option value="">${esc(allLabel)}</option>${entries.map(x=>`<option value="${esc(x.key)}">${esc(x.label)}</option>`).join('')}`;select.value=entries.some(x=>x.key===selected)?selected:'';return select.value}
-function refreshBaseLocalFilterControls(cases){const search=$('#baseLocalSearch'),year=$('#baseLocalYearFilter'),district=$('#baseLocalDistrictFilter'),place=$('#baseLocalPlaceFilter'),agroBtn=$('#baseLocalAgroFilter'),agroCount=$('#baseLocalAgroCount');if(search&&search.value!==baseLocalFilterState.query)search.value=baseLocalFilterState.query;baseLocalFilterState.year=setBaseLocalSelectOptions(year,L26FilterCore.yearEntries(cases),'Todos los años',baseLocalFilterState.year);const yearCases=L26FilterCore.filterByYear(cases,baseLocalFilterState.year),districts=baseLocalDistrictEntries(yearCases);baseLocalFilterState.district=setBaseLocalSelectOptions(district,districts,'Todos los distritos',baseLocalFilterState.district);const places=baseLocalPlaceEntries(yearCases,baseLocalFilterState.district);baseLocalFilterState.place=setBaseLocalSelectOptions(place,places,'Todos los lugares',baseLocalFilterState.place);const agroTotal=cases.filter(c=>isAgroObservationCase(c)||c.terrain?.agroDeclared==='Sí').length;if(agroCount)agroCount.textContent=String(agroTotal);if(agroBtn){agroBtn.setAttribute('aria-pressed',baseLocalFilterState.agroOnly?'true':'false');agroBtn.title=agroTotal?`Mostrar solo ${agroTotal} expediente(s) con uso agro`:'No hay expedientes agro indicados todavía.'}}
-function filterBaseLocalCases(cases){const query=normalizeBaseLocalFilterText(baseLocalFilterState.query),district=baseLocalFilterState.district,place=baseLocalFilterState.place,yearCases=L26FilterCore.filterByYear(cases,baseLocalFilterState.year);return yearCases.filter(c=>{if(query&&!baseLocalCaseSearchText(c).includes(query))return false;if(district&&!processGeoNamesMatch(canonicalProcessGeoName(c?.visit?.district||'Sin distrito'),district))return false;if(place&&!processGeoNamesMatch(canonicalProcessGeoName(casePlace(c)||'Sin poblado o lugar',{place:true}),place))return false;if(baseLocalFilterState.agroOnly&&!(isAgroObservationCase(c)||c.terrain?.agroDeclared==='Sí'))return false;return true})}
-function updateBaseLocalFilterStatus(visible,total){const status=$('#baseLocalFilterStatus');if(!status)return;const active=Boolean(normalizeBaseLocalFilterText(baseLocalFilterState.query)||baseLocalFilterState.year||baseLocalFilterState.district||baseLocalFilterState.place||baseLocalFilterState.agroOnly);const agroText=baseLocalFilterState.agroOnly?' Filtro de uso agro activo.':'';status.textContent=active?`Mostrando ${visible} de ${total} expediente(s) según la búsqueda y los filtros seleccionados.${agroText}`:`Mostrando los ${total} trámite(s) activos de Fiscalización, sin trasladar a Gestión.`}
+function refreshBaseLocalFilterControls(cases){const search=$('#baseLocalSearch'),year=$('#baseLocalYearFilter'),district=$('#baseLocalDistrictFilter'),place=$('#baseLocalPlaceFilter'),agroBtn=$('#baseLocalAgroFilter'),agroCount=$('#baseLocalAgroCount'),alarmBtn=$('#baseLocalAlarmFilter'),alarmCount=$('#baseLocalAlarmCount');if(search&&search.value!==baseLocalFilterState.query)search.value=baseLocalFilterState.query;baseLocalFilterState.year=setBaseLocalSelectOptions(year,L26FilterCore.yearEntries(cases),'Todos los años',baseLocalFilterState.year);const yearCases=L26FilterCore.filterByYear(cases,baseLocalFilterState.year),districts=baseLocalDistrictEntries(yearCases);baseLocalFilterState.district=setBaseLocalSelectOptions(district,districts,'Todos los distritos',baseLocalFilterState.district);const places=baseLocalPlaceEntries(yearCases,baseLocalFilterState.district);baseLocalFilterState.place=setBaseLocalSelectOptions(place,places,'Todos los lugares',baseLocalFilterState.place);const agroTotal=cases.filter(c=>isAgroObservationCase(c)||c.terrain?.agroDeclared==='Sí').length,alarmTotal=cases.filter(caseNeedsAgeAlarm).length;if(agroCount)agroCount.textContent=String(agroTotal);if(agroBtn){agroBtn.setAttribute('aria-pressed',baseLocalFilterState.agroOnly?'true':'false');agroBtn.title=agroTotal?`Mostrar solo ${agroTotal} expediente(s) con uso agro`:'No hay expedientes agro indicados todavía.'}if(alarmCount)alarmCount.textContent=String(alarmTotal);if(alarmBtn){alarmBtn.setAttribute('aria-pressed',baseLocalFilterState.alarmOnly?'true':'false');alarmBtn.title=alarmTotal?`Mostrar solo ${alarmTotal} trámite(s) con alarma de más de un año`:'No hay trámites activos con alarma de más de un año.'}}
+function filterBaseLocalCases(cases){const query=normalizeBaseLocalFilterText(baseLocalFilterState.query),district=baseLocalFilterState.district,place=baseLocalFilterState.place,yearCases=L26FilterCore.filterByYear(cases,baseLocalFilterState.year),alarmCases=L26FilterCore.filterByAlarm(yearCases,baseLocalFilterState.alarmOnly,caseNeedsAgeAlarm);return alarmCases.filter(c=>{if(query&&!baseLocalCaseSearchText(c).includes(query))return false;if(district&&!processGeoNamesMatch(canonicalProcessGeoName(c?.visit?.district||'Sin distrito'),district))return false;if(place&&!processGeoNamesMatch(canonicalProcessGeoName(casePlace(c)||'Sin poblado o lugar',{place:true}),place))return false;if(baseLocalFilterState.agroOnly&&!(isAgroObservationCase(c)||c.terrain?.agroDeclared==='Sí'))return false;return true})}
+function updateBaseLocalFilterStatus(visible,total){const status=$('#baseLocalFilterStatus');if(!status)return;const active=Boolean(normalizeBaseLocalFilterText(baseLocalFilterState.query)||baseLocalFilterState.year||baseLocalFilterState.district||baseLocalFilterState.place||baseLocalFilterState.agroOnly||baseLocalFilterState.alarmOnly);const agroText=baseLocalFilterState.agroOnly?' Filtro de uso agro activo.':'',alarmText=baseLocalFilterState.alarmOnly?' Filtro de alarmas +1 año activo.':'';status.textContent=active?`Mostrando ${visible} de ${total} expediente(s) según la búsqueda y los filtros seleccionados.${agroText}${alarmText}`:`Mostrando los ${total} trámite(s) activos de Fiscalización, sin trasladar a Gestión.`}
 
 async function renderCaseList(){if(!db&&storageMode!=='memory')return;const processHost=$('#caseList'),summary=$('#caseHierarchySummary'),all=(await loadCasesWithTerritoryInference()).sort(compareCasesByDistrictPlaceDate),localCases=all.filter(c=>!managementInspectionCompleted(c)),transferredCases=all.filter(c=>managementInspectionCompleted(c)),summaryCounts=L26ManagementCore.summaryCounts(all,managementInspectionCompleted),districts=buildProcessCaseHierarchy(localCases),placeGroupCount=[...districts.values()].reduce((n,d)=>n+d.places.size,0),alarmCount=localCases.filter(c=>caseNeedsAgeAlarm(c)).length;refreshBaseLocalFilterControls(localCases);const visibleCases=filterBaseLocalCases(localCases),emptyMessage=localCases.length?'No hay expedientes que coincidan con la búsqueda o los filtros seleccionados.':'No hay trámites activos de Fiscalización pendientes de traslado a Gestión.';$('#processCaseCount').textContent=L26IntegrityCore.uniqueFolioCount(visibleCases);updateBaseLocalFilterStatus(L26IntegrityCore.uniqueFolioCount(visibleCases),L26IntegrityCore.uniqueFolioCount(localCases));if(summary)summary.innerHTML=`<div><b>${summaryCounts.total}</b><span>Trámites</span></div><div class="summary-process"><b>${summaryCounts.active}</b><span>Activos</span></div><div class="summary-completed"><b>${summaryCounts.management}</b><span>En Gestión</span></div><div><b>${districts.size}</b><span>Distritos</span></div><div><b>${placeGroupCount}</b><span>Grupos de población</span></div><div class="summary-alarm"><b>${alarmCount}</b><span>Alarmas +1 año</span></div>`;await renderCaseHierarchy(processHost,visibleCases,emptyMessage);if(!localCases.length&&summary&&transferredCases.length)summary.insertAdjacentHTML('beforeend',`<p class="transferred-note">${transferredCases.length} expediente(s) permanecen resguardados en Gestión.</p>`);await renderManagementList()}
 $('#refreshCasesBtn').addEventListener('click',renderCaseList);
@@ -3247,6 +3247,7 @@
 $('#baseLocalDistrictFilter')?.addEventListener('change',event=>{baseLocalFilterState.district=event.target.value;baseLocalFilterState.place='';renderCaseList()});
 $('#baseLocalPlaceFilter')?.addEventListener('change',event=>{baseLocalFilterState.place=event.target.value;renderCaseList()});
 $('#baseLocalAgroFilter')?.addEventListener('click',()=>{baseLocalFilterState.agroOnly=!baseLocalFilterState.agroOnly;renderCaseList()});
+$('#baseLocalAlarmFilter')?.addEventListener('click',()=>{baseLocalFilterState.alarmOnly=!baseLocalFilterState.alarmOnly;renderCaseList()});
 $('#managementSearch')?.addEventListener('input',e=>{managementFilterState.query=e.target.value;renderManagementList()});
 $('#managementYearFilter')?.addEventListener('change',e=>{managementFilterState.year=e.target.value;managementFilterState.district='';managementFilterState.place='';renderManagementList()});
 $('#managementDistrictFilter')?.addEventListener('change',e=>{managementFilterState.district=e.target.value;managementFilterState.place='';renderManagementList()});
--- sw.js cache key ---
const CACHE='fiscalizacion-bi-l26-manual-20260829-croquis-proportional-v1-year-filter-v1-integridad-recuperacion-v1-internal-web-reader-v1-requested-updates-v1-conformidad-v1-windows-desktop-launch-v1-desktop-bridge-fallback-v2-pdf-compat-v1-compact-districts-v1-android-offline-pdf-ocr-v1-rebuild-reader-v2-metro-protocol-handoff-v1-folio-dedupe-posesoria-v1-global-backup-historical-consolidation-v1-metro-formula-links-v1-web-fallback-v1-alarm-filter-v1-release-27.3.9';
--- tests added ---
'use strict';
const test=require('node:test');
const assert=require('node:assert/strict');
const core=require('../app/assets/l26_filter_core.js');

test('filterByAlarm conserva todos los casos cuando el filtro está apagado',()=>{
  const cases=[{id:1},{id:2},{id:3}];
  const result=core.filterByAlarm(cases,false,item=>item.id===2);
  assert.deepEqual(result.map(x=>x.id),[1,2,3]);
  assert.notEqual(result,cases);
});

test('filterByAlarm muestra solo trámites con alarma cuando está activo',()=>{
  const cases=[{id:1},{id:2},{id:3}];
  const result=core.filterByAlarm(cases,true,item=>item.id!==2);
  assert.deepEqual(result.map(x=>x.id),[1,3]);
});
from pathlib import Path
ROOT=Path(__file__).resolve().parents[1]
HTML=(ROOT/'index.html').read_text(encoding='utf-8')
ALT=(ROOT/'Fiscalizacion_BI_V27_FINAL.html').read_text(encoding='utf-8')

def test_alarm_filter_is_present_only_in_tramites_quick_filters():
    assert 'id="baseLocalAlarmFilter"' in HTML
    assert 'id="baseLocalAlarmCount"' in HTML
    assert 'ALARMAS +1 AÑO' in HTML
    assert 'id="managementAlarmFilter"' not in HTML

def test_alarm_filter_uses_existing_alarm_rule_and_composes_with_other_filters():
    assert "baseLocalFilterState={query:'',year:'',district:'',place:'',agroOnly:false,alarmOnly:false}" in HTML
    assert 'L26FilterCore.filterByAlarm(yearCases,baseLocalFilterState.alarmOnly,caseNeedsAgeAlarm)' in HTML
    assert "baseLocalFilterState.alarmOnly=!baseLocalFilterState.alarmOnly" in HTML
    assert "baseLocalFilterState.alarmOnly?' Filtro de alarmas +1 año activo.':''" in HTML

def test_alarm_filter_entrypoints_stay_identical():
    assert HTML == ALT
